<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_dialog"
    android:padding="28dp"
    android:gravity="center">

    <!-- Reel icon -->
    <ImageView
        android:id="@+id/ivRenderIcon"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:src="@drawable/reels"
        android:layout_marginBottom="18dp"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tvRenderTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Preparing Reel"
        android:textColor="@color/colorPrimary"
        android:textSize="17sp"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:layout_marginBottom="6dp"/>

    <!-- Status message -->
    <TextView
        android:id="@+id/tvRenderStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Starting..."
        android:textColor="@color/color_green"
        android:textSize="13sp"
        android:gravity="center"
        android:layout_marginBottom="20dp"/>

    <!-- Progress bar -->
    <ProgressBar
        android:id="@+id/renderProgressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:max="100"
        android:progress="0"
        android:progressTint="@color/colorPrimary"
        android:progressBackgroundTint="@color/grey_1"
        android:layout_marginBottom="8dp"/>

    <!-- Percent + step row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvRenderStep"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Processing..."
            android:textColor="@color/grey_3"
            android:textSize="12sp"/>

        <TextView
            android:id="@+id/tvRenderPercent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0%"
            android:textColor="@color/colorPrimary"
            android:textSize="12sp"
            android:fontFamily="sans-serif-medium"/>

    </LinearLayout>

</LinearLayout>